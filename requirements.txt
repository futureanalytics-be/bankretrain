# BankRetain — Python dependencies
# Install with: pip install -r requirements.txt

# ── Azure SDKs ────────────────────────────────────────────────────────────────
azure-identity>=1.15
azure-ai-ml>=1.16
azure-storage-blob>=12.19
azure-keyvault-secrets>=4.7
azure-mgmt-eventgrid>=10.3

# ── Data / ML ─────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
pyarrow>=13.0
python-tds
scikit-learn>=1.3
lightgbm>=4.0

# ── MLflow (AML uses this for experiment tracking and model registry) ──────────
mlflow>=2.13,<3.0.0

# ── Plotting (training artefacts + dashboard) ─────────────────────────────────
matplotlib>=3.7
seaborn>=0.13

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.35
plotly>=5.20
